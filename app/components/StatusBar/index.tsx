export default function StatusBar() {
    return <div className={`flex flex-row py-0.75 border-t border-(--border) justify-between items-center-safe text-xs bg-[var(--accent)] px-2`}>
        Status
    </div>
}